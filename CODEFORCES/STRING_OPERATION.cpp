#include<stdio.h>
#include<string.h>
#include<ctype.h>
int main()
{
	char s[100];
	scanf("%s",s);
	char c;
	char s1[100];
	int t,n,f;
	scanf("%d",&t);
	while(t--)
	{
		scanf("%d",&n)
		if(n==1)
		{
			strrev(s);
		}
		else
		{
			scanf("%d%c",&f,&c);
			if(f==1)
			{
				
			}
			else
			{
				
			}
		}
	}
}
